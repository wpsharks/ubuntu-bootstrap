#!/usr/bin/env bash

if is-4ci; then

  # Resolve these additional host names.

  echo '127.0.0.1 sub.'"${CFG_HOST}" >> /etc/hosts;
  echo '127.0.0.1 sub1.'"${CFG_HOST}" >> /etc/hosts;
  echo '127.0.0.1 sub2.'"${CFG_HOST}" >> /etc/hosts;

else echo 'Skipping CI extra host names.'; fi;
